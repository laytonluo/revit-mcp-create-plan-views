# SRS — 結構樑翻模 PyRevit 外掛
## Software Requirements Specification

**文件版本：** 1.4  
**日期：** 2026-06-10  
**專案代號：** BEAM_MODELING  
**開發環境：** PyRevit / IronPython / Revit API  

---

## 1. 專案概述

### 1.1 目的
開發一個 PyRevit 外掛按鈕，讓使用者能從 Revit 平面視圖中已連結的 CAD 底圖（DWG）自動讀取結構樑中心線與標籤，並在 Revit 模型中建立對應的結構樑族群實例。

### 1.2 背景
目前作業流程依賴 Dynamo 節點組（`BEAM_CREATED_BY_CAD.dyn`）搭配 bimorph 第三方套件執行翻模。此外掛將取代 Dynamo 流程，實現一鍵操作，並支援團隊統一佈署。

### 1.3 適用範圍
- Revit 版本：2023 以上
- PyRevit 版本：4.8 以上
- 作業系統：Windows 10 / 11
- 不依賴 Dynamo、bimorph 或其他第三方軟體

---

## 2. 系統架構

### 2.1 技術棧
| 元件 | 技術 |
|---|---|
| UI 框架 | WPF（XAML + IronPython） |
| CAD 文字解析 | netDxf.netstandard.dll（打包進 Extension） |
| CAD 幾何讀取 | Revit API `FilteredElementCollector` + `ImportInstance` |
| DXF 產生 | Revit API `Document.Export`（自動執行，使用者無感） |
| 樑建立 | Revit API `doc.Create.NewFamilyInstance` |

### 2.2 佈署方式
```
BEAM_MODELING.extension/
├── BEAM_MODELING.panel/
│   └── CreateBeam.pushbutton/
│       ├── script.py
│       ├── ui/
│       │   ├── MainWindow.xaml
│       │   └── AddTypeDialog.xaml
│       └── lib/
│           └── netDxf.netstandard.dll
```
團隊成員只需將 Extension 資料夾放入 PyRevit Extensions 路徑，無需額外安裝任何套件。

---

## 3. 使用者流程

```
[Step 1] 設定面板
    使用者設定 CAD 連結、樓層、圖層對應、尺寸來源
         ↓ 點「執行分析」
    程式自動匯出 DXF → 解析幾何與文字
         ↓
[Step 2] 預覽確認表
    顯示所有偵測到的樑，使用者確認族群類型對應
    可新增缺少的族群類型
    可設定將梁編號寫入指定實例參數
         ↓ 點「確認建立」
[Step 3] 建立完成
    顯示建立結果摘要
```

---

## 4. 功能需求

### 4.1 Step 1 — 設定面板

#### 4.1.1 CAD 連結選擇
- 自動掃描目前 Revit 視圖中的所有 `ImportInstance`（CAD 連結）
- 以下拉選單列出，顯示格式：`檔案名稱.dwg`
- 預設選取第一個找到的 CAD 連結

#### 4.1.2 啟動前置判斷與樓層取得
- 外掛啟動時，先判斷目前作用中視圖是否為平面視圖
- **允許的視圖類型：** `FloorPlan`、`StructuralPlan`、`CeilingPlan`
- **不允許的視圖類型：** 剖面、立面、3D 視圖等
- 若非平面視圖，顯示錯誤訊息並結束：
  > 「請先切換至樓層平面視圖再執行此工具」
- 樓層自動取得目前平面視圖的關聯樓層（`view.GenLevel`），不提供下拉選單

#### 4.1.3 圖層對應設定
共三組，每組包含三個下拉選單：

| 組別 | 梁線圖層 | 編號圖層 | 尺寸圖層 |
|---|---|---|---|
| 大梁（垂直） | 下拉 | 下拉 | 下拉 |
| 大梁（水平） | 下拉 | 下拉 | 下拉 |
| 小梁 | 下拉 | 下拉 | 下拉 |

- 下拉選單內容：由程式掃描 CAD 連結後取得的所有圖層名稱
- **自動預填邏輯：**

| 圖層角色 | 命名關鍵字規則 |
|---|---|
| 梁線 | 含 `A2`（不含 `_SB`）→ 大梁；含 `A2_SB` → 小梁 |
| 編號 | 含 `AN-G` → 垂直大梁；含 `AN-B` → 水平大梁；含 `AN`（單獨）→ 小梁 |
| 尺寸 | 含 `A5-G` → 垂直大梁；含 `A5-B` → 水平大梁；含 `A5`（單獨）→ 小梁 |

- 大梁垂直與水平可指向同一梁線圖層（程式依角度分離）

#### 4.1.4 尺寸來源
三選一 RadioButton：

**① 從圖層讀取（預設）**
- 尺寸圖層下拉選單正常啟用
- 從 DXF 對應圖層讀取 MText 內容作為尺寸

**② 查對照表（CSV）**
- 尺寸圖層下拉選單反灰
- 自動彈出檔案選擇視窗（`.csv` 篩選）
- 顯示已選取的 CSV 路徑

**③ 全手動選取**
- 尺寸圖層下拉選單反灰
- Step 2 的「偵測尺寸」欄改顯示 **CAD 底圖兩條平行梁線之間的距離（即梁寬，單位 cm）**

---

### 4.2 Step 2 — 預覽確認表

#### 4.2.1 表格欄位
| 欄位 | 說明 |
|---|---|
| 梁編號 | 從 CAD 編號圖層讀取（如 `G1`、`B1`） |
| 方向/類別 | 垂直大梁 / 水平大梁 / 小梁(水平) / 小梁(垂直) |
| 偵測尺寸 | 從圖層或 CSV 取得的尺寸字串；全手動時顯示梁線長度 |
| 對應族群類型 | 下拉選單，列出專案中所有結構樑族群類型 |
| 狀態 | ✅ 已配對 / ⚠️ 未找到類型 |

#### 4.2.2 族群類型自動配對規則
1. 將尺寸字串正規化：去括號、轉小寫、統一分隔符為 `x`
   - `(60X85)` → `60x85`
   - `60*75` → `60x75`
2. 掃描專案中所有結構樑族群類型名稱，同樣正規化
3. 比對數字關鍵字，忽略前綴（B、RC 等）、後綴（cm 等）、大小寫
4. 找到第一個符合者自動選取

#### 4.2.3 水平/垂直角度判斷
- 線段向量角度 ±1° 內 → 水平
- 線段向量角度 90° ±1° 內 → 垂直
- 其餘角度 → 斜樑，標示特殊狀態

#### 4.2.4 小梁方向判斷
- 讀取 CAD 文字的 `Rotation` 屬性
- Rotation ≈ 0° 或 180° → 小梁（水平）
- Rotation ≈ 90° 或 270° → 小梁（垂直）

#### 4.2.5 暫時顯示梁中心線
- 執行分析後，以 `DirectShape` 在 Revit 視圖中顯示偵測到的梁中心線
- 關閉視窗或點「確認建立」後自動刪除，不留存於模型

#### 4.2.6 梁編號寫入實例參數
- 勾選框：`☑ 將梁編號填入實例參數`
- 勾選後啟用下拉選單，列出所選結構樑族群的**文字類型（String）實例參數**
- 建立樑時，將梁編號（如 `G1`）寫入所選參數
- 僅寫編號，不含尺寸

#### 4.2.7 未找到族群類型的處理
- 標示 ⚠️ 警告
- 確認建立時略過該筆，不建立
- 底部摘要列出略過數量

---

### 4.3 新增梁類型彈窗

點擊 **[+ 新增梁類型]** 觸發：

**輸入欄位：**
| 欄位 | 說明 |
|---|---|
| 複製自 | 下拉，列出專案現有結構樑族群類型 |
| 建議名稱 | 程式依尺寸字串自動帶入（如 `B60X85cm`） |
| 名稱 | 可手動修改 |
| 寬度 b | cm，自動從尺寸解析帶入 |
| 深度 h | cm，自動從尺寸解析帶入 |

**參數名稱偵測（寬度/深度）：**

程式掃描族群所有參數，依以下別名清單自動配對：

| 角色 | 偵測關鍵字（不分大小寫）|
|---|---|
| 寬度 | `梁寬`、`寬度`、`b`、`width` |
| 深度 | `梁深`、`深度`、`h`、`depth` |

若同時偵測到多個符合參數，跳出提示讓使用者選擇一次，後續沿用。

**建立後：**
- 新類型自動出現在確認表的下拉選單並選中對應列

---

### 4.4 Step 3 — 建立完成

#### 4.4.1 建立後圖形標記

對於確認表中狀態為**配對失敗（⚠️）** 或**手動選取族群類型**的樑，建立後在目前平面視圖套用圖形覆寫：

| 覆寫屬性 | 設定值 |
|---|---|
| 表面填滿圖樣 | 實心填滿（Solid Fill） |
| 填滿顏色 | RGB (255, 128, 64) |

**API 範例：**
```python
ogs = OverrideGraphicSettings()
fill_color = Color(255, 128, 64)
ogs.SetSurfaceForegroundPatternColor(fill_color)
ogs.SetSurfaceForegroundPatternVisible(True)
# 取得 Solid Fill FillPatternElement Id
solid_pattern_id = get_solid_fill_pattern_id(doc)
ogs.SetSurfaceForegroundPatternId(solid_pattern_id)
view.SetElementOverrides(beam_element_id, ogs)
```

> **判斷條件：** 確認表的「對應族群類型」欄位是否由使用者手動選取（非自動配對）；  
> 自動配對成功（✅）者不套用覆寫，保留原始顯示。

#### 4.4.2 結果摘要視窗

建立完成後跳出對話框：

```
建立完成

  ✅ 成功建立：X 隻
      其中手動選取：Y 隻（已標示橘色）

  ⚠️ 略過（未找到族群類型）：Z 根

  [確認]
```

- **X**：本次成功建立的結構樑總數
- **Y**：其中配對失敗或手動選取族群類型的數量（已套用橘色覆寫）
- **Z**：略過未建立的數量（族群類型留空者）

---

## 4.5 梁中心線生成機制

### 4.5.1 前提說明
CAD 底圖中每根樑以**兩條平行邊緣線**表示。懸臂樑會有額外第三條線段（懸挑部分）。程式需先配對雙線，計算中心線，再與標籤配對。

### 4.5.2 前處理：幾何線段正規化

在配對演算法執行前，先將所有幾何物件轉為單純的 `Line` 清單：

| 原始幾何類型 | 處理方式 |
|---|---|
| `Line` | 直接加入清單 |
| `PolyLine`（段數 ≤ 3） | 拆解為多條 `Line` 加入清單 |
| `PolyLine`（段數 > 3） | **排除**，視為非樑幾何 |
| `Arc` | **排除**，目前版本不處理弧形樑 |

> **實測資料（A2 圖層）：**  
> Line × 50、PolyLine × 8（2段×7條、3段×1條），Arc × 2  
> → 排除 2 條 Arc，PolyLine 全數拆解，共取得約 67 條有效 Line

---

### 4.5.3 配對演算法

```
Step A：依角度分組
    所有 Line 依角度分為三組：
    - 水平組：角度 ±1° 以內
    - 垂直組：角度 90° ±1° 以內
    - 斜樑組：其餘（保留，目前版本不配對，僅警告）

Step B：在同組內尋找等長平行線對
    對每條線 L1，尋找符合以下條件的 L2：
    ① 長度相等（誤差容許值 ≤ 5mm）
    ② 兩線垂直距離在合理梁寬範圍內（20cm ～ 120cm）
    ③ 投影重疊長度 ≥ L1 長度的 80%

    注意：L1 與 L2 起終點需確認同向，
    若反向則自動翻轉 L2 再計算。

Step C：排除懸臂梁的第三條線
    若某條線無法配對（無等長的平行線），
    判定為懸臂延伸線，暫時略過（保留警告）。
    → 目前版本：懸臂段不納入翻模，僅建立主跨中心線。

Step D：計算中心線
    對每個配對 (L1, L2)：
    - 起點 = L1.StartPoint 與 L2.StartPoint 連線中點
    - 終點 = L1.EndPoint 與 L2.EndPoint 連線中點
    - 中心點 = 中心線起終點的中點（用於標籤配對）
    - 梁寬 = L1 與 L2 之間的垂直距離（cm）

Step E：輸出
    每組配對 → 一條中心線 + 梁寬（cm） + 中心點座標
```

### 4.5.4 容許誤差參數（可未來調整）
| 參數 | 預設值 |
|---|---|
| 角度容許差 | ±1° |
| 長度容許差 | ±5 mm |
| 最小梁寬 | 20 cm |
| 最大梁寬 | 120 cm |
| 最小投影重疊比例 | 80% |

### 4.5.5 標籤與中心線配對（反向配對法）

**設計原則：以標籤為主動方，找最近的中心線；天然避免一對多重複配對。**

```
Step 1：對每個編號標籤 L，找同方向（水平/垂直）中心線中
        距離最近的中心點 M → 配對成功
        若多個標籤搶同一條中心線，保留距離最近者

Step 2：未被任何標籤配對的中心線 → 標示「未配對」
        並附註「最近標籤: XXX（已被占用）」供使用者參考

Step 3：尺寸標籤（A5-G / A5-B）亦採相同反向配對邏輯
```

**距離計算：** 標籤 Position 座標 到 中心線中心點 M 的 2D 歐式距離（cm）

**異常處理：**
- 未配對中心線 → Step 2 確認表顯示 ⚠️，族群類型欄留空，使用者手動選取
- 若 CAD 原圖標籤缺漏（非程式錯誤），同樣顯示 ⚠️ 提示

---

### 4.5.6 暫不處理的情況（留待後續版本）
- 懸臂梁延伸段的中心線
- 斜樑（非正交）的配對
- Arc 弧形樑

---

## 5. 技術規格

### 5.1 DXF 自動匯出
- 從 `CADLinkType.GetExternalFileReference()` 取得 DWG 路徑
- 以 `Document.Export()` 將目前視圖匯出為 DXF（ACADVersion.R2010）
- 匯出檔名：`{原始DWG名稱}_TEMP.dxf`，儲存於系統暫存資料夾
- 外掛結束時自動刪除暫存 DXF

### 5.2 DXF 文字解析
- 載入打包的 `netDxf.netstandard.dll`
- CAD 文字位於 Block `{DWG名稱}-{ElementId}-{ViewName}` 內的 `MText` 實體
- 依圖層名稱篩選目標文字
- 取得屬性：`Value`（文字內容）、`Position`（XY 座標）、`Rotation`（旋轉角度）

### 5.3 尺寸字串正規化
```python
import re
def normalize_size(raw):
    s = raw.strip().lower()
    s = s.replace("(", "").replace(")", "")
    s = re.sub(r"[xX\*×]", "x", s)
    return s  # e.g. "60x85"

def parse_size_cm(normalized):
    parts = normalized.split("x")
    return int(parts[0]), int(parts[1])  # width_cm, depth_cm
```

### 5.4 CSV 解析
- 編碼：UTF-8（支援 BOM）
- 必要欄位：第一欄 = 梁編號、第二欄 = 尺寸字串
- 第一行為標題列，自動略過
- 尺寸字串套用 5.3 正規化規則

### 5.5 單位換算
- UI 顯示：cm
- Revit API 輸入：feet（1 cm = 1/30.48 feet）
- 換算公式：`feet = cm / 30.48`

### 5.6 樑建立 API
```python
doc.Create.NewFamilyInstance(
    curve,          # 梁中心線（Line 或 Arc，單位 feet）
    family_symbol,  # FamilySymbol（結構樑族群類型）
    level,          # Level
    StructuralType.Beam
)
```

### 5.7 座標系統轉換

**轉換公式：**
```python
def cad_to_revit(x_mm, y_mm, transform):
    # CAD 世界座標 (mm) → Revit 內部座標 (feet)
    pt = transform.OfPoint(XYZ(x_mm / 304.8, y_mm / 304.8, 0))
    return pt  # XYZ in feet
```

**取得 Transform：**
```python
transform = import_instance.GetTransform()
# 包含偏移、旋轉、縮放，Transform.OfPoint() 一次處理
```

**實測驗證（FL11 視圖）：**

| 項目 | X (mm) | Y (mm) |
|---|---|---|
| 結構柱範圍 | -14,463 ～ +11,187 | -15,353 ～ -7,823 |
| G1 標籤轉換後 | -15,317 | -11,172 |
| G13 標籤轉換後 | +10,233 | -12,453 |

轉換後座標與結構柱範圍吻合，驗證通過 ✅

---

## 6. CSV 對照表格式規範

```csv
編號,尺寸
G1,(60X85)
G2,60X85
B1,(60x85)
b3a,60*75
```

**支援的尺寸格式：**
- `(60X85)`、`60X85`、`60x85`、`60*75`、`(60*75)`

---

## 7. 非功能需求

| 項目 | 規格 |
|---|---|
| 佈署方式 | 複製 Extension 資料夾，零安裝 |
| 外部相依 | 僅 `netDxf.netstandard.dll`（已打包） |
| Revit 版本 | 2023 以上 |
| 語言 | IronPython 3（PyRevit 預設） |
| 錯誤處理 | 所有例外捕捉並顯示友善訊息，不中斷 Revit |
| 暫存清理 | 暫存 DXF 與 DirectShape 於外掛結束時自動清除 |

---

## 8. 已驗證項目（MCP 測試記錄）

| 項目 | 結果 |
|---|---|
| 偵測 CAD 連結（ImportInstance） | ✅ |
| 取得 DWG 檔案路徑（CADLinkType） | ✅ |
| Revit API 匯出 DXF（Document.Export） | ✅ |
| 載入 netDxf.netstandard.dll | ✅ 版本 2.4.0 |
| 掃描 DXF 圖層列表 | ✅ 含 A5-G、AN-G、A5-B、AN-B |
| 讀取 Block 內 MText 文字 | ✅ |
| 讀取 A5-G 尺寸文字 | ✅ `(60x85)`、`(60x75)` |
| 讀取 AN-G 編號文字 | ✅ `G1`～`G16` |
| 讀取 AN-B 編號文字 | ✅ `B1`～`B16`、`b3a` |
| 讀取 MText Position 座標 | ✅ |
| 讀取 A2 圖層線段幾何 | ✅ 50 Line + 8 PolyLine + 2 Arc |
| A2 PolyLine 段數 | ✅ 最多 3 段，全數可拆解 |
| CAD Transform 偏移讀取 | ✅ `ImportInstance.GetTransform()` |
| 座標轉換驗證 | ✅ 轉換後與結構柱範圍吻合 |
| 雙重 Transform 問題修正 | ✅ `GetInstanceGeometry()` 已為世界座標，不再套 Transform |
| 輸出單位確認 | ✅ 座標輸出 cm（feet × 30.48），Dynamo 驗證正確 |
| A2 中心線配對演算法 | ✅ 32 條中心線，Dynamo 位置驗證正確 |
| A2_SB 中心線配對演算法 | ✅ 28 條中心線，3 條未配對（懸臂梁） |
| 反向標籤配對演算法 | ✅ 31/32 筆配對，1 筆為 CAD 原圖標籤缺漏（G11） |
| 標籤驗證（B13/B14/B15） | ✅ 最上排水平樑，左到右順序正確 |
| 標籤驗證（G1～G4） | ✅ 最左側垂直樑，下到上順序正確 |
| 標籤驗證（G13～G16） | ✅ 最右側垂直樑，下到上順序正確 |

---

## 9. 目前開發狀態與待處理項目

### 9.1 整體進度

| 階段 | 項目 | 狀態 |
|---|---|---|
| **需求** | SRS 文件 | ✅ 完成（v1.2）|
| **需求** | 技術驗證（MCP 測試） | ✅ 完成 |
| **設計** | WPF UI 架構（XAML） | 🔲 待開始 |
| **實作** | Step 1 設定面板 | 🔲 待開始 |
| **實作** | DXF 匯出與解析模組 | 🔲 待開始 |
| **實作** | 中心線配對演算法 | ✅ 驗證完成 |
| **實作** | 標籤距離配對（反向配對法） | ✅ 驗證完成 |
| **實作** | Step 2 預覽確認表 | 🔲 待開始 |
| **實作** | 新增梁類型彈窗 | 🔲 待開始 |
| **實作** | 樑建立（Revit API） | 🔲 待開始 |
| **測試** | 整合測試（實際 DWG）| 🔲 待開始 |

### 9.2 已解決的技術問題

| 問題 | 解決方案 |
|---|---|
| DWG 文字無法透過標準幾何 API 讀取 | Revit 匯出 DXF + netDxf 解析 MText |
| bimorph DLL 需 Dynamo 包裝型別 | 改用 netDxf 直接讀 DXF，不依賴 bimorph |
| CAD 座標系統與 Revit 不同 | DXF 文字座標套 `ImportInstance.GetTransform()`；Revit 幾何已為世界座標不需套 Transform |
| DXF 文字在 Block 內非頂層 | 讀取 Block `BEAM_...-{ElementId}-{ViewName}` 內的 MText |
| 標籤重複配對至多條中心線 | 改用反向配對法：以標籤為主動方找最近中心線 |
| 中心線無對應標籤（CAD 原圖缺漏）| 顯示 ⚠️ 未配對 + 最近已占用標籤提示 |

### 9.3 尚待決策的設計問題

| 項目 | 說明 | 狀態 |
|---|---|---|
| DirectShape 視覺樣式 | 暫時顯示中心線的顏色與線寬 | 🔲 待確認 |
| 斜樑處理 | 目前版本僅警告略過，後續版本再支援 | 📋 已決定略過 |
| 懸臂梁 | 目前版本僅建立主跨，懸臂段略過 | 📋 已決定略過 |
| 參數偵測衝突 | 同時找到多個寬/深參數時的選擇 UI | 🔲 待實作時確認 |

### 9.4 下一步
1. **WPF 視窗架構設計**（XAML 骨架）
2. 開始撰寫 script.py 主程式骨架

---

*文件版本 1.4 — 2026-06-10*
